services:
  vaultwarden:
    image: vaultwarden/server:latest
    container_name: vaultwarden
    restart: unless-stopped
    environment:
      DOMAIN: "https://vault.homelab118.home"
    volumes:
      - ./vw-data/:/data/
    ports:
      - 8000:80
